{
    "version": "0.2.0",
    "configurations": [                
        {
            "name": "Debug (CMake + LLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "cwd": "${workspaceFolder}/ODFAEG2Editor",
            "stopAtEntry": false
        }
    ]
}